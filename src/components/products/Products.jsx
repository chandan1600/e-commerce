import React from 'react';
import Grid  from '@material-ui/core';
import { Description } from '@material-ui/icons';

const products = {
    { id: 1, name: 'Shoes', Description: 'Air Jordans.'},

};

const Products = () => {
    <main>
        <Grid container justify="center" spacing={4}>

        </Grid>
    </main>
}

export default Products;